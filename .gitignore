# Python
__pycache__/
*.pyc
venv/
.venv/

# 自動產生 / 可重新生成的東西
logs/
outputs/*.png
data/fonts/*.otf          # 中文字型 16MB，font_util 會自動下載，不需進版控
data/dataset.csv          # 可由 data_collection 重新抓取
data/wf_signals.csv       # walk-forward 訊號快取

# 系統
.DS_Store

# 注意：frontend/ 整個保留（含 index.html、support.js、predictions.json、backtest.json）
#       因為那就是要拿去靜態托管的網站內容。
